#include <stdio.h>
int main(void)
{
    int secret = 13;
    int code;

    printf("naleale:");
    scanf("%d", &code);
    while (code != secret)
    {
        printf("nalaljdla:");
        scanf("%d", &code);
        /* code */
    }
    printf("congratulation!");
}